# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a simple JavaScript repository containing basic demonstration code.

## Running Code

Execute the main script:
```bash
node hello.js
```

## Architecture

This is a minimal single-file JavaScript project with no build system or dependencies.

- 모든 장소의 'admission_fee'는 반드시 원화(KRW) 단위를 포함하여 문자열로 명시한다. 숫자로만 되어 있으면 안 된다.